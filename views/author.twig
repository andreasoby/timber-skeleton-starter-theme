{% extends "base.twig" %}
<div class="container">
    {% for post in posts %}
    <div class="row">
        <div class="twelve columns">{% include ["tease-"~post.post_type~".twig", "tease.twig"] %}</div>
    </div>
    {% endfor %}
</div>
{% endblock %}
